[project]
name = "simple-module-core"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "Anto Subash", email = "antosubash@live.com" }
]
requires-python = ">=3.12"
dependencies = [
    "babel>=2.14",
    "fastapi>=0.115",
    "packaging>=23.0",
    "pydantic>=2.0",
    "pydantic-settings>=2.0",
    "pyee>=12.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

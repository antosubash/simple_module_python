services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: simple_module
      POSTGRES_USER: sm
      POSTGRES_PASSWORD: sm
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
